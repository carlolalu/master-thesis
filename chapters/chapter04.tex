\section{Del Dini derivativation of $J_t^N$}

In the main result we will need to consider the del Dini derivation of $\sup_{0 \leq t \leq T} \norm{\sol_t^N-\mfsol_t^N}_\infty$.

\subsection{Del Dini derivatives and FTC}
[[define and present the del dini derivative]]

\subsection{Derivate the specific function}

Consider the random function

\begin{equation}
\Zhi(t) := | \sol_t^N - \mfsol_t^N|_\infty
\end{equation}

Since the sample paths of $\sol_t^N$ are $\Cspace^0$ ach path of $\Zhi$ i

can be considered pathwise
has these properties:

\begin{enumerate}
\item $g$ is \cadlag
\item $g$ has a finite number of discontinuouities on $[0,T]$
\item $\forall t. \; g(t-) \leq g(t)$ (notice that $g$ is not necessarily increasing!)
\end{enumerate}

\section{The two laws of large numbers}

\section{The main result}